## 需要实现的功能

1. **非武器资源在右手骨骼 Prop_R 上实例化模型**
   - 修改 `PlayerWeaponController.OnWeaponIdChanged` 方法
   - 当 `newId <= 0` 时，根据 `localSlotIndex` 获取当前选中的资源
   - 使用 `attachmentSystem.AttachWeapon` 挂载资源模型到 Prop_R

2. **选中非武器资源时阻止攻击**
   - 修改 `WeaponStateManager.UpdateWeaponData` 方法
   - 当 `weaponId < 0` 时，将 `currentWeaponData` 设为 null
   - 这样 `ServerHandleAttackRequest` 中的检查就能阻止攻击

## 具体修改

**文件1**: PlayerWeaponController.cs
- 修改 `OnWeaponIdChanged` 方法，当 `newId <= 0` 时调用新的 `EquipResourceVisual` 方法
- 添加 `EquipResourceVisual` 方法，根据 `localSlotIndex` 获取资源并挂载

**文件2**: WeaponStateManager.cs
- 修改 `UpdateWeaponData` 方法，当 `weaponId < 0` 时设置 `currentWeaponData = null`