## 修改内容

移除 `hasBufferedComboRequest` 相关代码，简化攻击逻辑：

### 1. 删除变量声明
```csharp
// 删除这一行
private bool hasBufferedComboRequest = false;
```

### 2. 修改 ServerHandleAttackRequest
移除"情况2：正在播放攻击动画，缓存连击请求"的代码块

### 3. 修改 ServerStartAttackStage
移除 `hasBufferedComboRequest = false;`

### 4. 修改 ServerOnAttackAnimationEnd
移除缓存检查逻辑，只保留连击窗口逻辑

### 5. 修改 ServerResetCombo
移除 `hasBufferedComboRequest = false;`

## 简化后的攻击流程

1. **Idle 状态 + 点击** → 开始 A1
2. **A1 播放中 + 点击** → 忽略（无缓存）
3. **连击窗口期 + 点击** → 开始下一段
4. **连击窗口超时** → 重置

这样逻辑更清晰，不会有缓存和连击窗口的冲突。