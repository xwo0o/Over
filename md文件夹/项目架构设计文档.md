# Over 项目 - 架构设计文档

---

## 📋 目录
- [一、整体架构概览](#一整体架构概览)
- [二、核心系统架构](#二核心系统架构)
- [三、网络架构](#三网络架构)
- [四、数据流设计](#四数据流设计)
- [五、模块依赖关系](#五模块依赖关系)
- [六、设计模式应用](#六设计模式应用)
- [七、技术选型决策](#七技术选型决策)

---

## 一、整体架构概览

### 1.1 架构分层图

```
┌─────────────────────────────────────────────────────────┐
│                    View 层（UI）                         │
│  InventoryUIController │ HealthBarUI │ BuildingUI       │
└─────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────┐
│                Controller 层（逻辑）                     │
│  NetworkPlayer │ PlayerWeaponController │ EnemyAIController│
└─────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────┐
│                  Model 层（数据）                        │
│  Inventory │ CharacterStats │ WeaponDatabase            │
└─────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────┐
│                  基础设施层                                │
│  AutoObjectPoolManager │ EventBus │ Addressables         │
└─────────────────────────────────────────────────────────┘
```

### 1.2 核心设计原则

1. **服务器权威**：所有游戏逻辑在服务器端执行
2. **MVC分离**：数据、视图、逻辑清晰分离
3. **模块解耦**：通过事件总线减少直接依赖
4. **配置驱动**：游戏数据通过JSON配置管理
5. **性能优先**：对象池、Dictionary缓存等优化

---

## 二、核心系统架构

### 2.1 玩家系统架构

```
NetworkPlayer（核心容器）
├── CharacterStats（血量、属性）
├── Inventory（背包数据）
├── CharacterMovementController（移动控制）
├── PlayerWeaponController（武器控制）
├── WeaponDamageSystem（伤害系统）
├── CharacterModelManager（模型管理）
└── SceneAwareAnimatorManager（动画管理）
```

**职责划分：**
- **NetworkPlayer**：协调各个子系统，处理网络同步
- **CharacterStats**：管理角色属性（血量、速度）
- **Inventory**：管理背包物品
- **PlayerWeaponController**：处理武器切换和输入
- **WeaponDamageSystem**：处理伤害判定和连击

### 2.2 敌人系统架构

```
EnemyAIController（AI控制）
├── EnemyHealthManager（血量管理）
├── NavMeshAgent（寻路）
└── Animator（动画）
```

**状态机设计：**
```
Patrol ←───────────┐
   ↓               │
Chase ────────→ Attack
   ↓               │
   └──────→ Dead ←─┘
```

### 2.3 对象池系统架构

```
AutoObjectPoolManager（总管理器）
├── AutoObjectPool（单个对象池）
│   ├── Queue<GameObject> availableObjects（可用对象）
│   └── List<GameObject> activeObjects（活跃对象）
└── Dictionary<string, AutoObjectPool> pools（所有池）
```

**关键设计：**
- 使用Queue存储可用对象（FIFO，公平使用）
- 使用List存储活跃对象（便于遍历）
- Dictionary快速查找对象池

---

## 三、网络架构

### 3.1 Mirror Networking 架构

```
服务器端
├── NetworkServer（服务器核心）
├── [Server] 标记的方法
├── Command 接收客户端请求
└── ClientRpc 广播到所有客户端

客户端
├── NetworkClient（客户端核心）
├── SyncVar 自动同步
├── Command 发送请求到服务器
└── ClientRpc 接收服务器广播
```

### 3.2 同步机制

#### SyncVar + Hook 模式

```csharp
// 服务器修改数据
[SyncVar(hook = nameof(OnHealthChanged))]
public int currentHealth;

// Hook函数（所有客户端调用）
void OnHealthChanged(int oldValue, int newValue)
{
    if (isLocalPlayer)  // 只在本地玩家刷新UI
    {
        UpdateHealthBar(newValue);
    }
}
```

**关键点：**
- SyncVar只检测引用变化，数组需要克隆
- Hook函数在所有客户端调用，需要判断isLocalPlayer
- 服务器修改数据 → SyncVar同步 → Hook刷新UI

#### SyncList + struct 模式（背包同步优化）

```csharp
// ⚠️ 重要：InventoryItem 必须是 struct（值类型）
[System.Serializable]
public struct InventoryItem
{
    public string resourceId;
    public int amount;
}

public class Inventory : NetworkBehaviour
{
    // 使用 SyncList 实现增量同步
    public readonly SyncList<InventoryItem> slots = new SyncList<InventoryItem>();
    
    public override void OnStartClient()
    {
        // 订阅 Callback 事件
        slots.Callback += OnSlotsChanged;
    }
    
    void OnSlotsChanged(SyncList<InventoryItem>.Operation op, int index, InventoryItem oldItem, InventoryItem newItem)
    {
        // 数据变化时刷新UI
        RefreshUI();
    }
}
```

**为什么 InventoryItem 必须是 struct？**
- struct 是值类型，修改 `slots[i] = slot` 会替换整个元素
- SyncList 能检测到元素替换，触发增量同步
- 如果用 class（引用类型），修改字段不会改变引用，SyncList 无法检测变化
- 这是 Mirror SyncList 的核心限制

**SyncList vs SyncVar数组对比：**
| 特性 | SyncVar数组 | SyncList |
|------|-------------|----------|
| 同步方式 | 全量同步 | 增量同步 |
| GC压力 | 高（每次克隆数组） | 低（无克隆） |
| 网络带宽 | 大（整个数组） | 小（只同步变化） |
| 代码复杂度 | 高（需手动触发） | 低（自动检测） |
| 元素类型 | 任意 | 必须是 struct |

#### Command 模式

```csharp
// 客户端调用
[Command(requiresAuthority = true)]
void CmdRequestWeaponSwitch(int slotIndex)
{
    // 服务器验证
    if (!IsValidSlot(slotIndex)) return;
    
    // 服务器执行
    currentWeaponId = GetWeaponId(slotIndex);
}
```

**关键点：**
- requiresAuthority = true：只有对象所有者能调用
- 服务器验证参数，防止作弊
- 服务器执行后，通过SyncVar同步结果

#### ClientRpc 模式

```csharp
// 服务器调用
[ClientRpc]
void RpcPlayAttackAnimation()
{
    // 所有客户端执行
    animator.SetTrigger("IsAtk");
}
```

**关键点：**
- 服务器广播到所有客户端
- 用于播放动画、特效等不需要验证的操作

### 3.3 网络时序问题解决方案

#### 问题1：后加入客户端看不到已加载的模型

**解决方案：**
```
本地玩家加载完成
    ↓
CmdNotifyModelLoaded() → 服务器
    ↓
服务器设置 modelLoaded = true（SyncVar）
    ↓
RpcNotifyModelLoaded() → 所有客户端
    ↓
所有客户端初始化动画器
```

#### 问题2：对象池激活时NetworkIdentity未初始化

**解决方案：**
- OnEnable()中检查NetworkServer.active
- 重新获取组件引用
- 检查isDead标志判断是否从池中重新激活

---

## 四、数据流设计

### 4.1 背包数据流向

```
玩家拖拽物品
    ↓
InventorySlotUI（检测拖拽）
    ↓
CmdSwapSlots(index1, index2) → 服务器
    ↓
服务器验证并交换
    ↓
slots[index1] = tempItem（SyncList自动检测变化）
    ↓
SyncList增量同步到所有客户端
    ↓
Callback回调触发
    ↓
RefreshUI()（刷新UI）
```

**优化亮点：SyncList替代SyncVar数组**
- **增量同步**：只同步变化的元素，而非整个背包
- **零GC压力**：移除数组克隆操作，减少内存分配
- **代码简化**：直接修改元素，无需手动触发同步

### 4.2 伤害数据流向

```
玩家攻击
    ↓
动画事件触发
    ↓
CmdOnAttackHit() → 服务器
    ↓
WeaponDamageSystem.PerformDamageCheck()
    ↓
OverlapSphere检测敌人
    ↓
CmdApplyDamage(netId, damage) → 服务器
    ↓
EnemyHealthManager.ApplyDamage()
    ↓
currentHealth = newHealth（SyncVar）
    ↓
OnHealthChanged() Hook
    ↓
血条UI更新
    ↓
如果血量≤0 → Die()
    ↓
归还到对象池
```

### 4.3 武器切换数据流向

```
玩家滚动滚轮
    ↓
HandleLocalInput()（客户端）
    ↓
立即更新本地UI（即时反馈）
    ↓
CmdRequestWeaponSwitch(slotIndex) → 服务器
    ↓
服务器验证：
  - 槽位合法？
  - 玩家有该武器？
    ↓
currentWeaponId = weaponId（SyncVar）
    ↓
SyncVar自动同步到所有客户端
    ↓
OnWeaponIdChanged() Hook
    ↓
所有客户端更新武器模型
```

---

## 五、模块依赖关系

### 5.1 依赖图

```
NetworkPlayer
├── 依赖 → CharacterStats
├── 依赖 → Inventory
├── 依赖 → PlayerWeaponController
│   └── 依赖 → WeaponDatabase
│   └── 依赖 → Inventory
├── 依赖 → WeaponDamageSystem
│   └── 依赖 → WeaponDatabase
│   └── 依赖 → EnemyHealthManager
└── 依赖 → CharacterModelManager

EnemyAIController
├── 依赖 → EnemyHealthManager
├── 依赖 → EnemyDatabase
└── 依赖 → AutoObjectPoolManager

AutoObjectPoolManager
├── 依赖 → ResourceDatabase
└── 依赖 → Addressables

数据库系统（无外部依赖）
├── WeaponDatabase
├── EnemyDatabase
├── ResourceDatabase
└── CharacterDatabase
```

### 5.2 依赖原则

1. **高层模块不依赖低层模块**：都依赖抽象
2. **数据库系统是底层**：不依赖任何业务模块
3. **NetworkPlayer是协调者**：依赖多个子系统
4. **尽量减少循环依赖**：通过事件总线解耦

---

## 六、设计模式应用

### 6.1 单例模式

**应用场景：**
- AutoObjectPoolManager
- WeaponDatabase
- EnemyDatabase
- ResourceDatabase

**实现：**
```csharp
public class WeaponDatabase : MonoBehaviour
{
    public static WeaponDatabase Instance { get; private set; }
    
    void Awake()
    {
        if (Instance != null && Instance != this)
        {
            Destroy(gameObject);
            return;
        }
        Instance = this;
        DontDestroyOnLoad(gameObject);
    }
}
```

### 6.2 对象池模式

**应用场景：**
- 敌人生成和回收
- 资源生成和回收

**实现要点：**
- Queue存储可用对象
- List存储活跃对象
- 重置方法ResetPoolState()

### 6.3 状态机模式

**应用场景：**
- 敌人AI（巡逻→追击→攻击→死亡）

**实现要点：**
- 枚举定义状态
- Update中根据当前状态执行不同逻辑
- 状态转换条件判断

### 6.4 MVC模式

**应用场景：**
- 背包系统
- 角色系统

**实现要点：**
- Model：数据存储（Inventory）
- View：UI显示（InventoryUIController）
- Controller：逻辑协调（Inventory）

### 6.5 观察者模式（事件总线）

**应用场景：**
- 模块间通信

**实现要点：**
- Subscribe/Unsubscribe/Publish
- Dictionary<事件类型, 回调列表>

---

## 七、技术选型决策

### 7.1 为什么选择 Mirror Networking？

| 对比项 | Mirror | Photon | 自建 |
|--------|--------|--------|------|
| 开源 | ✅ | ❌ | ✅ |
| 免费 | ✅ | ❌（有付费版） | ✅ |
| 学习成本 | 低 | 中 | 高 |
| 功能完整 | ✅ | ✅ | ❌ |
| 社区活跃 | ✅ | ✅ | ❌ |

**选择理由：**
1. 开源免费，适合学习和独立开发
2. 与Unity深度集成
3. API简洁易懂
4. 社区活跃，文档完善

### 7.2 为什么选择 JSON 配置？

| 对比项 | 硬编码 | ScriptableObject | JSON |
|--------|--------|------------------|------|
| 可读性 | 差 | 中 | 好 |
| 不需要重编译 | ❌ | ✅ | ✅ |
| 版本控制友好 | ❌ | ⚠️ | ✅ |
| 外部工具编辑 | ❌ | ❌ | ✅ |

**选择理由：**
1. 策划可以直接编辑，不需要程序员参与
2. Git版本控制友好，diff清晰
3. 热更友好，未来可以运行时重新加载
4. 跨平台，纯文本

### 7.3 为什么选择 Dictionary 缓存？

**选择理由：**
1. 查询O(1)，比List快得多
2. 适合按ID查找的场景
3. 内存占用可接受

** tradeoff：**
- 按其他字段查询需要遍历
- 但项目中主要是按ID查询，利大于弊

### 7.4 为什么选择服务器权威架构？

**选择理由：**
1. 防作弊：关键数据由服务器控制
2. 同步一致性：所有客户端看到相同状态
3. 代码维护：逻辑只写一份

** tradeoff：**
- 网络延迟影响手感（需要客户端预测）
- 服务器成本较高
- 但对于多人游戏，这是必要的

---

## 📝 总结

本项目的架构设计遵循以下原则：

1. **清晰分层**：MVC架构，数据、视图、逻辑分离
2. **模块解耦**：通过事件总线减少直接依赖
3. **性能优先**：对象池、Dictionary缓存等优化
4. **网络可靠**：服务器权威，SyncVar+Hook机制
5. **配置驱动**：JSON配置，灵活易扩展

作为一个多人联机动作RPG项目，这个架构已经足够清晰和完整，覆盖了Unity游戏开发的核心技术点。
