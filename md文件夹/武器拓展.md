这是完整的总结，针对你的**单层动画控制器**设计（攻击时不能移动）。

---

## 🎯 整体架构

```
单层动画控制器 (Base Layer Only)
├── Locomotion (2D Blend Tree: Speed × WeaponType)
│   ├── (0,0): Idle_Kongshou (空手待机)
│   ├── (1,0): Walk_Kongshou (空手行走)
│   ├── (2,0): Run_Kongshou (空手奔跑)
│   ├── (0,1): Idle_WGS (阔刀待机)
│   ├── (1,1): Walk_WGS (阔刀行走)
│   ├── (2,1): Run_WGS (阔刀奔跑)
│   ├── (0,2): Idle_WTD (双刀待机)
│   ├── (1,2): Walk_WTD (双刀行走)
│   ├── (2,2): Run_WTD (双刀奔跑)
│   ├── (0,3): Idle_Axe (斧头待机)
│   ├── (1,3): Walk_Axe (斧头行走)
│   └── (2,3): Run_Axe (斧头奔跑)
│
└─ 攻击子状态机 (通过Any State进入)
    ├── 武器判断 (WeaponType_int)
    │   ├── WT=1 → WGS (阔刀三段连击)
    │   ├── WT=2 → WTD (双刀三段连击)
    │   └── WT=3 → Axe (斧头单次持续伤害)
    │
    └── 阔刀/双刀连击结构（使用AttackStage参数）
        Entry → A1 ──[AttackStage==2]──► A2 ──[AttackStage==3]──► A3 ──► Exit
                └─[AttackStage==0]──► Exit        └─[AttackStage==0]──► Exit
```

---

## ⚔️ 武器配置

| 武器ID | 武器名称 | 类型 | 连击段数 | 特殊机制 |
|--------|---------|------|---------|---------|
| 0 | Kongshou (空手) | 无 | 1段 | 无特殊 |
| 1 | WGS (阔刀/大刀) | 近战 | **3段连击** | 每段独立伤害 |
| 2 | WTD (双刀) | 近战 | **3段连击** | 每段独立伤害，第三段多hit |
| 3 | Axe (斧头) | 近战 | 1段 | **单次动画3次伤害** |

### 连击时间窗口配置

| 武器 | 攻击段 | 窗口开启时机 | 窗口关闭时机 | 窗口持续时间 |
|------|--------|-------------|-------------|-------------|
| 阔刀 | A1→A2 | 动画开始 | 动画结束后0.3s | 动画时长+0.3s |
| 阔刀 | A2→A3 | 动画开始 | 动画结束后0.3s | 动画时长+0.3s |
| 双刀 | A1→A2 | 动画开始 | 动画结束后0.3s | 动画时长+0.3s |
| 双刀 | A2→A3 | 动画开始 | 动画结束后0.3s | 动画时长+0.3s |
| 斧头 | 无 | - | - | - |

**说明**：
- **新机制**：连击窗口从动画播放开始即开启，直到动画结束后0.3秒才关闭
- 玩家可以在动画播放期间**随时**触发连击，不需要等待特定时间点
- 动画结束后仍有0.3秒的缓冲时间可以触发连击
- 代码控制：`comboWindowDuration = 0.3f`

---

## 🎬 二维动画混合 (Locomotion)

### Blend Tree配置
- **Type**: 2D Freeform Cartesian
- **X参数**: `speed` (Float, 0~1)
  - 0 = 待机
  - 0.5 = 行走
  - 1 = 奔跑
- **Y参数**: `Weapon` (Float, 0~3)
  - 0 = 空手
  - 1 = 阔刀
  - 2 = 双刀
  - 3 = 斧头

### 12个动画片段
| 坐标 | 动画名 | 说明 |
|------|--------|------|
| (0,0) | Idle_Kongshou | 空手待机 |
| (1,0) | Walk_Kongshou | 空手行走 |
| (2,0) | Run_Kongshou | 空手奔跑 |
| (0,1) | Idle_WGS | 阔刀待机 |
| (1,1) | Walk_WGS | 阔刀行走 |
| (2,1) | Run_WGS | 阔刀奔跑 |
| (0,2) | Idle_WTD | 双刀待机 |
| (1,2) | Walk_WTD | 双刀行走 |
| (2,2) | Run_WTD | 双刀奔跑 |
| (0,3) | Idle_Axe | 斧头待机 |
| (1,3) | Walk_Axe | 斧头行走 |
| (2,3) | Run_Axe | 斧头奔跑 |

---

## ⚡ 攻击子状态机设计（新版：使用AttackStage参数）

### 入口分发 (Dispatch State)
```
Any State ──[WT=1 & AttackStage>0]──► WGS_SubStateMachine
Any State ──[WT=2 & AttackStage>0]──► WTD_SubStateMachine  
Any State ──[WT=3 & AttackStage>0]──► Axe_SubStateMachine
```

### 阔刀/双刀三段连击结构（使用AttackStage）
```
Sub-State Machine (WGS/WTD)
│
├─ Entry → A1 (第一段动画，AttackStage=1)
│   ├─[AttackStage==2]──────────────────────► A2 (第二段)
│   └─[AttackStage==0]──────────────────────► Exit (返回Idle)
│
├─ A2 (第二段动画，AttackStage=2)
│   ├─[AttackStage==3]──────────────────────► A3 (第三段)
│   └─[AttackStage==0]──────────────────────► Exit (返回Idle)
│
├─ A3 (第三段动画，AttackStage=3)
│   └─[AttackStage==0]──────────────────────► Exit (无条件返回)
│
└─ Exit → 回到Dispatch → Locomotion
```

**说明**：
- 使用 `AttackStage` Int 参数直接控制当前播放哪一段攻击动画
- `AttackStage = 1` → 播放第一段攻击
- `AttackStage = 2` → 播放第二段攻击
- `AttackStage = 3` → 播放第三段攻击
- `AttackStage = 0` → 返回 Idle 状态

### 斧头持续伤害结构
```
Sub-State Machine (Axe)
│
├─ Entry → Axe_Spin (单次旋转动画，AttackStage=1)
│   └─ 动画事件: AE_Hit(0) / AE_Hit(1)
│   └─[AttackStage==0]──────────────────────► Exit
│
└─ Exit → 回到Dispatch → Locomotion
```

---

## 📋 完整参数列表（新版）

| 参数名 | 类型 | 用途 | 取值范围 |
|--------|------|------|---------|
| `speed` | Float | 移动速度 | 0~2 (0待机/1行走/2奔跑) |
| `WeaponType` | Float | **Blend Tree混合** (浮点) | 0~3 |
| `WeaponType_int` | Int | **状态机等于判断** (整数) | 0/1/2/3 |
| `AttackStage` | **Int** | **攻击阶段控制** | 0=Idle, 1=A1, 2=A2, 3=A3 |

**注意**：
- 移除了 `Atk` Trigger
- 移除了 `ComboWindow` Bool
- 移除了 `ComboTrigger` Trigger
- 统一使用 `AttackStage` Int 参数控制攻击动画

---

## 🔗 Transition条件配置（新版）

### 入口线 (Any State → 子状态机)
| 条件 | 值 |
|------|---|
| WeaponType_int | Equals 1/2/3 |
| AttackStage | Greater 0 |

**Settings**:
- Has Exit Time: ❌ False
- Transition Duration: 0.1
- Can Transition To Self: ❌ False

### 连击线 (A1→A2)
| 条件 | 值 |
|------|---|
| AttackStage | Equals 2 |

**Settings**:
- Has Exit Time: ❌ False
- Transition Duration: 0.08
- Can Transition To Self: ❌ False

### 连击线 (A2→A3)
| 条件 | 值 |
|------|---|
| AttackStage | Equals 3 |

**Settings**:
- Has Exit Time: ❌ False
- Transition Duration: 0.08
- Can Transition To Self: ❌ False

### 返回线 (A1→Exit, A2→Exit, A3→Exit)
| 条件 | 值 |
|------|---|
| AttackStage | Equals 0 |

**Settings**:
- Has Exit Time: ✅ True (A1/A2: 0.9, A3: 1.0)
- Transition Duration: 0.15

---

## 💻 完整代码控制（新版）

```csharp
using UnityEngine;

[RequireComponent(typeof(Animator))]
public class WeaponAnimController : MonoBehaviour
{
    private Animator anim;
    
    // 参数Hash（性能优化）
    private static readonly int Speed = Animator.StringToHash("speed");
    private static readonly int Weapon = Animator.StringToHash("Weapon");
    private static readonly int WeaponTypeInt = Animator.StringToHash("WeaponType_int");
    private static readonly int AttackStage = Animator.StringToHash("AttackStage");
    
    [Header("移动设置")]
    [SerializeField] private float walkSpeed = 1f;
    [SerializeField] private float runSpeed = 2f;
    
    [Header("输入")]
    private float horizontalInput;
    private float verticalInput;
    private bool isAttacking;
    
    void Start()
    {
        anim = GetComponent<Animator>();
        InitializeWeapon();
    }
    
    void Update()
    {
        HandleMovementInput();
        HandleAttackInput();
        UpdateAnimatorParameters();
    }
    
    /// <summary>
    /// 处理移动输入
    /// </summary>
    void HandleMovementInput()
    {
        horizontalInput = Input.GetAxis("Horizontal");
        verticalInput = Input.GetAxis("Vertical");
    }
    
    /// <summary>
    /// 处理攻击输入
    /// </summary>
    void HandleAttackInput()
    {
        // 检测攻击键（鼠标左键或J键）
        if (Input.GetMouseButtonDown(0) || Input.GetKeyDown(KeyCode.J))
        {
            TryAttack();
        }
    }
    
    /// <summary>
    /// 尝试触发攻击
    /// </summary>
    void TryAttack()
    {
        // 已经在攻击中，尝试连击
        if (IsInAttackState())
        {
            // 在窗口期内，增加AttackStage进入下一段
            int currentStage = anim.GetInteger(AttackStage);
            if (currentStage < 3) // 最大3段
            {
                anim.SetInteger(AttackStage, currentStage + 1);
                Debug.Log($"连击衔接: Stage {currentStage} -> {currentStage + 1}");
            }
            else
            {
                Debug.Log("已达最大连击段数");
            }
        }
        // 不在攻击中，开始新攻击
        else
        {
            anim.SetInteger(AttackStage, 1);
            Debug.Log("开始攻击: Stage 1");
        }
    }
    
    /// <summary>
    /// 更新Animator参数
    /// </summary>
    void UpdateAnimatorParameters()
    {
        // 计算移动速度
        Vector3 input = new Vector3(horizontalInput, 0, verticalInput);
        float inputMagnitude = input.magnitude;
        
        // 映射到0~2 (待机/行走/奔跑)
        float animSpeed = inputMagnitude * runSpeed;
        anim.SetFloat(Speed, Mathf.Clamp(animSpeed, 0f, 2f));
        
        // 检测是否在攻击状态
        isAttacking = IsInAttackState();
    }
    
    /// <summary>
    /// 检查是否在攻击状态（Locomotion之外的状态）
    /// </summary>
    bool IsInAttackState()
    {
        // 获取当前状态信息
        AnimatorStateInfo stateInfo = anim.GetCurrentAnimatorStateInfo(0);
        
        // 检查是否在攻击子状态机中（通过状态名或标签判断）
        // 给所有攻击状态加Tag "Attack"，这里检查Tag
        return stateInfo.IsTag("Attack");
    }
    
    /// <summary>
    /// 初始化武器（默认空手）
    /// </summary>
    void InitializeWeapon()
    {
        SwitchWeapon(0); // 0=空手
    }
    
    /// <summary>
    /// 切换武器
    /// </summary>
    public void SwitchWeapon(int weaponId)
    {
        // 限制范围 0~3
        weaponId = Mathf.Clamp(weaponId, 0, 3);
        
        // 设置Blend Tree混合参数 (Float)
        anim.SetFloat(Weapon, weaponId);
        
        // 设置状态机判断参数 (Int)
        anim.SetInteger(WeaponTypeInt, weaponId);
        
        // 重置AttackStage
        anim.SetInteger(AttackStage, 0);
        
        Debug.Log($"切换武器: {GetWeaponName(weaponId)}");
    }
    
    /// <summary>
    /// 获取武器名称
    /// </summary>
    string GetWeaponName(int id)
    {
        return id switch
        {
            0 => "空手",
            1 => "阔刀",
            2 => "双刀",
            3 => "斧头",
            _ => "未知"
        };
    }
    
    /// <summary>
    /// 结束攻击（由动画事件调用）
    /// </summary>
    public void EndAttack()
    {
        // 重置AttackStage，让Animator返回Idle
        anim.SetInteger(AttackStage, 0);
        Debug.Log("攻击结束，AttackStage = 0");
    }
    
    /// <summary>
    /// 快捷键切换武器（测试用）
    /// </summary>
    void OnGUI()
    {
        if (GUILayout.Button("空手 [0]")) SwitchWeapon(0);
        if (GUILayout.Button("阔刀 [1]")) SwitchWeapon(1);
        if (GUILayout.Button("双刀 [2]")) SwitchWeapon(2);
        if (GUILayout.Button("斧头 [3]")) SwitchWeapon(3);
    }
}

/// <summary>
/// 动画事件脚本（挂在玩家身上，供Animation Event调用）
/// </summary>
public class ComboAnimEvents : MonoBehaviour
{
    private Animator anim;
    private static readonly int AttackStage = Animator.StringToHash("AttackStage");
    
    void Start()
    {
        anim = GetComponent<Animator>();
    }
    
    /// <summary>
    /// 动画事件：伤害判定（挂在所有攻击动画的伤害帧）
    /// </summary>
    /// <param name="hitIndex">伤害段数索引</param>
    public void AE_Hit(int hitIndex)
    {
        Debug.Log($"伤害判定: 第{hitIndex + 1}击");
        
        // 这里调用伤害检测逻辑
        // PerformDamageDetection(hitIndex);
    }
    
    /// <summary>
    /// 动画事件：攻击结束（挂在所有攻击动画的最后一帧）
    /// </summary>
    public void EndAttack()
    {
        // 重置AttackStage，让Animator返回Idle
        anim.SetInteger(AttackStage, 0);
        Debug.Log("动画事件: EndAttack，AttackStage = 0");
    }
}
```

---

## 🎬 Animation Event 时间轴配置

### 阔刀/双刀 A1动画（第一段攻击）
```
时间:    0.0s         0.2s                    0.8s~0.9s      0.9s+0.3s
         │            ↓                       │              │
         开始      AE_Hit(0)   EndAttack   连击窗口关闭
                     (伤害判定)   (动画结束)    (代码控制)
                 
注意: 
- 连击窗口从动画开始即开启，动画结束后0.3秒关闭
- 不需要添加AE_OpenWindow/AE_CloseWindow事件
- EndAttack事件设置AttackStage=0，让Animator返回Idle
```

### 阔刀/双刀 A2动画（第二段攻击）
```
时间:    0.0s         0.2s                    0.8s~0.9s      0.9s+0.3s
         │            ↓                       │              │
         开始      AE_Hit(0)   EndAttack   连击窗口关闭
                     (伤害判定)   (动画结束)    (代码控制)
```

### 阔刀/双刀 A3动画（第三段攻击 - 终结技）
```
时间:    0.0s         0.2s                    0.8s~0.9s      0.9s+0.3s
         │            ↓                       │              │
         开始      AE_Hit(0)   EndAttack   连击窗口关闭
                     (伤害判定)   (动画结束)    (代码控制)
                 
注意: 
- 第三段为终结技，无连击窗口（动画结束立即重置）
- 第三段只有1次伤害判定
- EndAttack事件设置AttackStage=0
```

### 斧头 Axe_Spin动画（单次动画多次伤害）
```
时间:    0.0s      0.25s         0.75s           1.0s
         │          ↓             ↓               │
         开始     AE_Hit(0)     AE_Hit(1)      EndAttack
                   (第1击)       (第2击)         (动画结束)
                 
注意: 
- 单次动画，2次独立伤害判定
- 斧头为单段攻击，动画结束立即重置，可直接再次攻击
- 无连击窗口概念
- EndAttack事件设置AttackStage=0
```

### 伤害判定说明

| 武器 | 攻击段 | 伤害次数 | AE_Hit参数 | 说明 |
|------|--------|---------|-----------|------|
| 阔刀 | A1 | 1次 | AE_Hit(0) | 第一段独立伤害 |
| 阔刀 | A2 | 1次 | AE_Hit(0) | 第二段独立伤害 |
| 阔刀 | A3 | 1次 | AE_Hit(0) | 第三段终结技 |
| 双刀 | A1 | 1次 | AE_Hit(0) | 第一段独立伤害 |
| 双刀 | A2 | 1次 | AE_Hit(0) | 第二段独立伤害 |
| 双刀 | A3 | 1次 | AE_Hit(0) | 第三段终结技 |
| 斧头 | Spin | 2次 | AE_Hit(0)/(1) | 单次动画2次伤害 |

---

## ✅ 关键检查清单（新版）

### 动画状态机配置
- [ ] 12个Locomotion动画片段正确放置于Blend Tree
- [ ] `speed`参数范围0~2，`Weapon`参数范围0~3
- [ ] `WeaponType_int`用于状态机等于判断（Int）
- [ ] `Weapon`用于Blend Tree混合（Float）
- [ ] **添加`AttackStage` Int参数（0~3）**
- [ ] **子状态机内A1→A2使用`AttackStage==2`**
- [ ] **子状态机内A2→A3使用`AttackStage==3`**
- [ ] **所有攻击状态返回Exit使用`AttackStage==0`**
- [ ] 所有攻击状态添加Tag "Attack"
- [ ] Transition设置`Can Transition To Self = False`
- [ ] 连击线Has Exit Time=False，返回线Has Exit Time=True

### 动画事件配置（重要！）
**新机制说明**：连击窗口由代码控制（动画开始即开启，结束后0.3秒关闭）

- [ ] **A1动画**：添加AE_Hit(0)(0.2s)、**EndAttack(动画结束)**
- [ ] **A2动画**：添加AE_Hit(0)(0.2s)、**EndAttack(动画结束)**
- [ ] **A3动画**：添加AE_Hit(0)(0.2s)、**EndAttack(动画结束)**
- [ ] **Axe动画**：添加AE_Hit(0)(0.25s)、AE_Hit(1)(0.75s)、**EndAttack(动画结束)**

**注意**：EndAttack事件会调用`anim.SetInteger("AttackStage", 0)`让Animator返回Idle

### 代码组件配置
- [ ] Player预制体添加WeaponStateManager组件
- [ ] Player预制体添加WeaponDamageSystem组件
- [ ] Player预制体添加WeaponAttachmentSystem组件
- [ ] Player预制体添加WeaponAnimEvents组件（挂在模型根节点）
- [ ] 配置攻击检测层级（LayerMask）
- [ ] 配置攻击检测范围和距离

### 参数清理（从旧版迁移）
- [ ] **删除`Atk` Trigger参数**
- [ ] **删除`ComboWindow` Bool参数**
- [ ] **删除`ComboTrigger` Trigger参数**
- [ ] **统一使用`AttackStage` Int参数**

---

## 🔄 新旧版本对比

| 功能 | 旧版（ComboWindow+ComboTrigger） | 新版（AttackStage） |
|------|----------------------------------|---------------------|
| 开始攻击 | `Atk` Trigger | `AttackStage = 1` |
| 连击衔接 | `ComboWindow=true` + `ComboTrigger` | `AttackStage = 2/3` |
| 返回Idle | `ComboWindow=false` | `AttackStage = 0` |
| 参数数量 | 3个（Atk, ComboWindow, ComboTrigger） | 1个（AttackStage） |
| 逻辑复杂度 | 高（需要管理Bool和Trigger） | 低（只用Int参数） |
| 可控性 | 一般 | 高（直接指定阶段） |

---

这套设计**单层架构清晰**，使用`AttackStage`参数直接控制攻击动画，逻辑更简单可控！

### 伤害判定流程
```
玩家按下攻击键
    ↓
WeaponStateManager.TryAttack()
    ↓
WeaponDamageSystem.StartAttack() - 增加连击阶段，重置伤害状态
    ↓
设置AttackStage = 1/2/3，播放对应攻击动画（A1/A2/A3）
    ↓
动画事件AE_Hit(0)触发
    ↓
WeaponStateManager.PerformDamageCheck()
    ↓
WeaponDamageSystem.PerformDamageCheck() - 检测范围内敌人并造成伤害
    ↓
动画结束EndAttack()触发
    ↓
设置AttackStage = 0，Animator返回Idle
    ↓
WeaponDamageSystem.EndAttack() - 开启连击窗口（如果不是最后一段）
```
